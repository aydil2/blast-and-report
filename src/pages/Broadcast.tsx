import { BottomNav } from "@/components/BottomNav";

const Broadcast = () => {
  return (
    <div className="min-h-screen bg-background pb-24">
      <div className="container mx-auto px-4 py-8">
        <h1 className="text-3xl font-bold text-foreground mb-6">Broadcast</h1>
        <p className="text-muted-foreground">Halaman broadcast akan segera hadir...</p>
      </div>
      <BottomNav />
    </div>
  );
};

export default Broadcast;
